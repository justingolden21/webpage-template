Change dist to docs, remove "move to docs" python script from tailwind PWA

Create template for jekyll tailwind site

Remove copy/paste files (python scripts only)

Test on linux

Add postcss-import to package.json and postcss.config.json (allows for @importing other css files in preprocessor) in both tailwind template and tailwind pwa template

Create setup for dynamically generating python scripts (share readme, index, etc among several scripts for easy updates in future)
